@model PatisserieCestBon.Models.Customer
@{
    Layout = "~/Views/Shared/_BeforeAuthCustomer.cshtml";
    ViewBag.title = "ログイン";
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <script type="text/javascript" src="~/Scripts/common.js"></script>
</head>
<body>
    <br>
    顧客IDとパスワードを入力してください。
    <br>
    @* ID・パスワードが空欄もしくは制約違反の場合
        （＝ViewBagにエラーメッセージが格納されている場合）
          エラーメッセージ表示 *@
    @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
    {
        <p class="text-danger">@ViewBag.ErrorMessage</p>
    }
    <br>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
<form action="/Login/CustomerLogin" method="post">
    <table align="center">
        <tr>
            <td style="text-align:left">顧客ID</td>
            <td><input type="text" name="inputCustomerId"></td>
        </tr>
        <tr>
            <td style="text-align:left">パスワード&nbsp;</td>
            <td><input type="password" name="inputPassword"></td>
        </tr>
    </table>
    <br>
    <input type="submit" value="ログイン">
    <input type="reset" value="クリア" onClick="clearFormAll();">
</form>
    }
    <br>
    初めてシステムをご利用になる方は、<br />
    まず
    @Html.ActionLink("システム利用登録", "CustomerAdd1", "Customer") @*顧客登録リンク *@
    をしていただく必要があります。<br /><br />
</body>
</html>